source('~/.active-rstudio-document', encoding = 'UTF-8')
source('C:/Users/user/Dropbox/R/R/thesis/TM1/try1.R', encoding = 'UTF-8')
rm(list=ls(all.names = TRUE))
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(rJava)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/user/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
mycorpus <- tm_map(mycorpus, removePunctuation)
mycorpus <- tm_map(mycorpus, stripWhitespace)
mycorpus <- tm_map(mycorpus, removeNumbers)
mycorpus <- tm_map(mycorpus, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
mycorpus1 <- tm_map(mycorpus, segmentCN, nature = TRUE)
?Analyzer
install.packages("Analyzer")
segmentCN(strwords, analyzer = get("Analyzer", envir = .RwordsegEnv)
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/user/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
mycorpus <- tm_map(mycorpus, removePunctuation)
mycorpus <- tm_map(mycorpus, stripWhitespace)
mycorpus <- tm_map(mycorpus, removeNumbers)
library(rJava)
mycorpus <- tm_map(mycorpus, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
mycorpus1 <- tm_map(mycorpus, segmentCN, nature = TRUE)
source('~/.active-rstudio-document', encoding = 'UTF-8')
rm(list=ls(all.names = TRUE))
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(rJava)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/ASUS/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
source('~/.active-rstudio-document', encoding = 'UTF-8')
rm(list=ls(all.names = TRUE))
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(rJava)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/user/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
mycorpus <- tm_map(mycorpus, removePunctuation)
mycorpus <- tm_map(mycorpus, stripWhitespace)
mycorpus <- tm_map(mycorpus, removeNumbers)
mycorpus <- tm_map(mycorpus, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
library(jiebaR)
cutter <- worker()
mycorpus1<-segment("mycorpus",cutter)
mycorpus2 <- Corpus(VectorSource(mycorpus1))
c<-data.frame(text = sapply(mycorpus2, as.character), stringsAsFactors = FALSE)
write.table(c,file="data2") #匯出檔案方便確認結果
source('~/.active-rstudio-document', encoding = 'UTF-8')
source('~/.active-rstudio-document', encoding = 'UTF-8')
source('~/.active-rstudio-document', encoding = 'UTF-8')
rm(list=ls(all.names = TRUE))
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(rJava)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/user/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
mycorpus <- tm_map(mycorpus, removePunctuation)
mycorpus <- tm_map(mycorpus, stripWhitespace)
mycorpus <- tm_map(mycorpus, removeNumbers)
mycorpus <- tm_map(mycorpus, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
cutter <- worker()
mycorpus<- Corpus(VectorSource(mycorpus))
mycorpus<-as.character(mycorpus)
mycorpus1<-segment(mycorpus,cutter)
mycorpus2 <- Corpus(VectorSource(mycorpus1))
c<-data.frame(text = sapply(mycorpus2, as.character), stringsAsFactors = FALSE)
write.table(c,file="data2") #匯出檔案方便確認結果
myStopWords <- c(toTrad(stopwordsCN()),"一下")
mycorpus3 <- tm_map(mycorpus2, removeWords, myStopWords)
c<-data.frame(text = sapply(mycorpus3, as.character), stringsAsFactors = FALSE)
write.table(c,file="data2X")
library(wordcloud)
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(2, Inf)))
m1 <- as.matrix(tdm)
v <- sort(rowSums(m1), decreasing = TRUE)
d <- data.frame(word = names(v), freq = v)
wordcloud(d$word, d$freq, min.freq = 10, random.order = F, ordered.colors = F,
colors = rainbow(length(row.names(m1))))
library(wordcloud)
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(2, Inf)))
m1 <- as.matrix(tdm)
source('~/.active-rstudio-document', encoding = 'UTF-8')
source('~/.active-rstudio-document', encoding = 'UTF-8')
rm(list=ls(all.names = TRUE))
library(tm)
library(tmcn)
library(Rwordseg)
library(jiebaR)
library(rJava)
library(SnowballC) # 似乎是上面某個套件的輔助
library(slam)  # 似乎是上面某個套件的輔助
m<-"C:/Users/user/Dropbox/R/R/thesis/TM1" #檔案路徑
mycorpus <- Corpus(DirSource(m),list(language = NA)) #讀取路徑下所有txt
mycorpus <- tm_map(mycorpus, removePunctuation)
mycorpus <- tm_map(mycorpus, stripWhitespace)
mycorpus <- tm_map(mycorpus, removeNumbers)
mycorpus <- tm_map(mycorpus, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
cutter <- worker()
mycorpus<- Corpus(VectorSource(mycorpus))
mycorpus<-as.character(mycorpus)
mycorpus1<-segment(mycorpus,cutter)
mycorpus2 <- Corpus(VectorSource(mycorpus1))
c<-data.frame(text = sapply(mycorpus2, as.character), stringsAsFactors = FALSE)
write.table(c,file="data2") #匯出檔案方便確認結果
myStopWords <- c(toTrad(stopwordsCN()),"一下")
mycorpus3 <- tm_map(mycorpus2, removeWords, myStopWords)
c<-data.frame(text = sapply(mycorpus3, as.character), stringsAsFactors = FALSE)
write.table(c,file="data2X")
mycorpus3 <- tm_map(mycorpus3, removePunctuation)
mycorpus3 <- tm_map(mycorpus3, stripWhitespace)
mycorpus3 <- tm_map(mycorpus3, removeNumbers)
mycorpus4 <- tm_map(mycorpus3, function(word) {
gsub("[A-Za-z0-9]", "", word)
})
library(wordcloud)
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(2, Inf)))
m1 <- as.matrix(tdm)
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(3, Inf)))
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(2, Inf)))
tdm <- TermDocumentMatrix(mycorpus3, control = list(wordLengths = c(3, Inf)))
m1 <- as.matrix(tdm)
